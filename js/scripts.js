/*
$(document).keydown(function (e) {
	  var keyCode = e.keyCode || e.which,
	      arrow = {left: 37, up: 38, right: 39, down: 40 };

	  switch (keyCode) {
	    case arrow.up:
	    	// up
	    break;
	    case arrow.down:
	    	// down
	    break;
	    case arrow.left:
	    	// left
	    break;
	    case arrow.right:
	    	// right
	    break;
	  }
});
*/